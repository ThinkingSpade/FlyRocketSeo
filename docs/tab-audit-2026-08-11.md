# Tab audit — 2026-08-11

Every project tab audited against Keyword Research, which is the app's
reference standard. Findings carry `file:line` so each one can be picked up on
its own. Everything under **Fixed** is on `main` with CI green; everything under
**Open** is verified but untouched.

The recurring shapes, in order of how much they cost the user:

1. **A failed or in-flight read renders as a confident absence.** The repo has
   fixed this at least four times with explanatory comments and it keeps coming
   back. Worst on metered tabs, where it also invites spend.
2. **A tab states a next step and cannot reach it.** Diagnosis and treatment
   live in different tabs, and the link runs the wrong way or not at all.
3. **A feature re-derives the client's brand from the domain stem** while the
   profile holds the curated spellings. Always wrong the same way, for the same
   clients.
4. **A parameter exists in the destination's schema and the link omits it**, so
   the handoff arrives blind.

---

## Fixed

| Tab                                                                     | What was wrong                                                                                                                                                                                                                                                                                                                         |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Local Rank Grid                                                         | Typing a location and pressing Scan ran nothing, then reported "Share of voice 0%, 0/25 pins". The geocode path stored full precision while `run.authorize` got a 4dp copy; `createMeteredRunKey` is `JSON.stringify`, so the keys never matched and every cell query stayed disabled. Clicking the map already rounded, which hid it. |
| SERP Overview                                                           | The entire results view was gated on `result && effectiveGeo`, so any run stored without a geo bundle — including every run launched from Overview's "run everything" card — rendered only a restored-run banner whose "Run again" re-bills.                                                                                           |
| Page Explorer                                                           | Billed the wrong country twice: defaulted to the US instead of the project market, and "Analyze my homepage" pinned to that default while the Location select sat beside it.                                                                                                                                                           |
| All 12 verdict tabs                                                     | `Action.to` was declared, rendered by `NextStepsCard`, and set by no builder. Every "what to do next" in the product was inert text.                                                                                                                                                                                                   |
| Keyword Research → Backlinks                                            | "See who links to them" passed no target; Backlinks resolves `kind: "domain"` and Keyword Research writes `kind: "keyword"`, so the chain fell through to `projectDefault` — you landed on **your own site**.                                                                                                                          |
| Domain Overview                                                         | "Full analysis" dropped the domain the card is about; rival rows were plain text.                                                                                                                                                                                                                                                      |
| Site Audit → On-Page Fixes                                              | The issues list names the four rewritable problems; On-Page Fixes rewrites them and linked back, but nothing linked forward.                                                                                                                                                                                                           |
| Saved Keywords                                                          | The empty state — the tab's whole first-run experience — named Keyword Research in prose with no link.                                                                                                                                                                                                                                 |
| Rank Tracking                                                           | A failed config read said "No tracked domains yet"; a failed trend read said "No history yet — run a check", steering toward spend.                                                                                                                                                                                                    |
| Site Audit verdict                                                      | Asserted "no Search Console click data is available" while that query was still loading, and forever if it failed.                                                                                                                                                                                                                     |
| Prompt Explorer, seed field, GSC branded split, Backlinks anchor health | All four derived brand identity from the domain stem while `resolveBrandTerms` sat with one consumer.                                                                                                                                                                                                                                  |
| Competitors                                                             | "Rows per page" was a one-option dropdown wired to a no-op.                                                                                                                                                                                                                                                                            |
| Client Report                                                           | Printed "FlyRocketSEO read this period's Search Console data" inside a chapter admitted by a snapshot, directly above "Search Console isn't connected"; blamed a missing GSC connection for a section fed by a different query; collapsed four distinct GSC failure reasons into "not connected".                                      |
| On-Page Fixes (generator)                                               | Wrote titles and meta with no idea what the client sells; an operator who only places vending machines could be handed "Buy Vending Machines".                                                                                                                                                                                         |
| Rank Tracking                                                           | The metered action carried no price while the cheap one beside it did; a billing blip failed an add whose keywords had already saved.                                                                                                                                                                                                  |
| Site Audit                                                              | One broken page was reported under five issue keys, inflating the verdict's traffic intersection and earning a dead URL an AI title rewrite.                                                                                                                                                                                           |
| Opportunities → Cannibalization                                         | "Review" passed no query because the destination had no `validateSearch`, dropping the user at the top of an unfiltered list of up to fifty cards.                                                                                                                                                                                     |
| Overview                                                                | The "run everything" card promised "each tab then opens showing its result" and linked to none of the eleven tabs it populated.                                                                                                                                                                                                        |
| Project switcher                                                        | Now shows each project's own site favicon.                                                                                                                                                                                                                                                                                             |
| Competitors                                                             | "Next page" and gap-mode switches de-authorized the run and blanked the table; page > 1 also issued two billed calls for the same endpoint.                                                                                                                                                                                            |
| Link Opportunities                                                      | Up to 150 external fetches of the client's own site fired on mount, before anyone had read a row.                                                                                                                                                                                                                                      |
| Site Audit                                                              | Three cache names per read, so a deleted audit lingered on five surfaces and each paid for its own copy; thin content double-counted broken pages; a failed status read asserted deletion.                                                                                                                                             |
| AI Visibility / Prompt Explorer                                         | "Recent searches" was a no-op — results survived the URL clearing; a hardcoded US market disagreed with the panel beside it; a failed lookup wiped stored trend data from view.                                                                                                                                                        |
| On-Page Fixes                                                           | Sorted by work remaining rather than traffic; wrote copy with no idea what the client sells; could not be linked into.                                                                                                                                                                                                                 |
| SEO Opportunities                                                       | Mounted the profile editor and ranked purely by traffic, so off-offer queries topped "what to fix next" and the headline summed unwanted traffic.                                                                                                                                                                                      |
| GSC Insights                                                            | Could not be deep-linked at all; its "rewrite the title and meta" guidance reached nothing.                                                                                                                                                                                                                                            |
| Saved Keywords                                                          | No relevance anywhere — "quick wins" meant low KD alone, so an off-target list reported a healthy portfolio.                                                                                                                                                                                                                           |
| Rank Tracking                                                           | Pre-selected 20 keywords by traffic and billed a recurring check on them; a failed billing read un-gated the paid action.                                                                                                                                                                                                              |
| Client Report                                                           | 11 of 13 reads could fail and print as "never run"; nine features left no trace at all.                                                                                                                                                                                                                                                |
| Client Report (chapters)                                                | Eight of those nine now have real chapters that either earn a sheet or name what to run; only Local Rank Grid is still uncovered, and for a stated reason.                                                                                                                                                                             |

---

## Open

The audit backlog is closed. What remains is design work, not wiring:

### One feature still has no Client Report chapter

Eight of the nine now have real chapters (`src/client/features/report/chapters/`,
wired through `chapters/index.ts`): Rank Tracking, Local SEO, Competitors, Topic
Clusters, Saved Keywords, Trends, SERP and Citations. Each either earns a sheet
or drops a coverage line naming what this project would need to run — so a
failed read can never print as work the agency skipped.

**Local Rank Grid stays uncovered, deliberately.** A grid scan is addressed by
its parameters, which live only in the tab's URL; its cache key is a digest of
them and there is no D1 row to enumerate. A chapter could only guess the
parameters, and a wrong guess returns an empty grid — byte-identical to a scan
that found the client ranking nowhere. Printing "you rank nowhere nearby"
because we could not find the scan is worse than printing nothing. Making the
scans addressable (a row per scan, keyed by project) is the prerequisite.

### Five routes still have no `validateSearch`

`opportunities`, `report`, and three others nothing currently links into. Add a
schema when something needs to link in; adding one speculatively just creates a
parameter no caller passes.

## Notes for whoever picks this up

- Verdict actions are now the cheapest way to tie two tabs together: add `to` in
  the builder, and `NextStepsCard` supplies `params={{ projectId }}`. The
  builders are pure and have no project in scope — don't thread one in.
- `ON_PAGE_FIXABLE` in `insights/verdicts/audit.ts` is shared by the verdict and
  the issues list so they can't drift about what On-Page Fixes can resolve.
- `resolveBrandTerms(profile, domain)` is the only correct source of brand
  strings. It unions the curated field with the domain stem and ignores an
  unconfirmed AI draft.
- `AnchorHealthCard` takes `brandTerms` as a prop rather than calling a hook: it
  renders through `renderToStaticMarkup` with no query client in its tests.
- A report chapter is one module in `report/chapters/` exporting
  `use<slug>ReportData` (its own reads) and `build<slug>Chapter` (pure, takes a
  `ChapterCollector`). Add both to `chapters/index.ts` and it is wired. Every
  read must be a restore or a stored-row query — the report renders on mount, so
  a metered call there spends the agency's money unasked.
- Chapter band numbers are printed on each sheet and `buildReportChapters` sorts
  the pages by them, so a builder can be called in any order. The number↔kicker
  pairing is 1:1; reusing another band's number splits one kicker across two
  numbers in the same PDF.
